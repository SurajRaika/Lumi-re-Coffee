    <style>
        .kanit-bold { font-family: 'Kanit', sans-serif; font-weight: 700; }
        .kanit-medium { font-family: 'Kanit', sans-serif; font-weight: 500; }
        .kanit-light { font-family: 'Kanit', sans-serif; font-weight: 300; }

        /* Limelight Font Class */
        .limelight-regular { 
            font-family: "Limelight", sans-serif; 
            font-weight: 400; 
            font-style: normal; 
        }
        
        /* Ensure responsive scaling works for the product images */
        .group:hover .product-image {
            transform: scale(1.02);
            transition: transform 0.3s ease-in-out;
        }
        
        /* Optional: Add slight background hover for card group */
        .group:hover {
            transition: background-color 0.3s ease-in-out;
            border-radius: 0.5rem; /* Match rounded-lg */
        }
        .seedling-badge {
            position: relative;
        }
        /* parent */
        .seedling-badge {
            position: relative;
            z-index: 0;             /* keep content above the pulse */
            --pulse-offset-x: -10px;    /* tweak this to move left/right */
            --pulse-offset-y: 0px;    /* tweak this to move up/down  */
            --pulse-scale: 2;         /* overall size of the ring */
            --pulse-opacity: 0.35;
        }

        /* background pulse */
        .seedling-badge::after {
            content: "";
            position: absolute;
            /* anchor at center so scaling + translate behave predictably */
            top: 50%;
            left: 50%;
            width: 120%;
            height: 120%;
            border-radius: 9999px;
            border: inherit;
            transform-origin: center;
            transition: scale 300ms ease;
            /* translate from center then scale; subtract offsets so positive moves left/up */
            transform: translate(calc(-50% - var(--pulse-offset-x)),
                                 calc(-50% - var(--pulse-offset-y)))
                         scale(var(--pulse-scale));
            opacity: var(--pulse-opacity);
            animation: pulseCircle 2000ms infinite ease-in-out;
            pointer-events: none;       /* doesn't block clicks */
            z-index: -1;                /* sits behind the badge content */
        }

        /* pulse animation */
        @keyframes pulseCircle {
            0%   { transform: translate(calc(-50% - var(--pulse-offset-x)), calc(-50% - var(--pulse-offset-y))) scale(var(--pulse-scale)); opacity: var(--pulse-opacity); }
            50%  { transform: translate(calc(-50% - var(--pulse-offset-x)), calc(-50% - var(--pulse-offset-y))) scale(calc(var(--pulse-scale) * 1.15)); opacity: 0.12; }
            100% { transform: translate(calc(-50% - var(--pulse-offset-x)), calc(-50% - var(--pulse-offset-y))) scale(var(--pulse-scale)); opacity: var(--pulse-opacity); }
        }
.group:hover .seedling-badge::after {
    scale:0;
}.group:hover .seedling-badge::after {
    scale:0;

}
    </style>
</head>

<!-- Floating Find My Flavor Button (Fixed Top-Right) -->
<!-- Wrapper div handles fixed position and 'group' for tooltip hover -->
<div class="fixed top-4 right-4 z-20 group  "> 
    <button 
    
    
  onclick="window.URL='https://chatgpt.com/g/g-p-6905ecf5db488191afe282db32982034-meet-lumi-your-personal-coffee-assistant/project?temporary-chat=true'" 
    
    
    
    title="Click to use our AI Coffee Assistant!" class="relative flex items-center justify-center  text-white rounded-full  duration-300 transform hover:scale-105  uppercase               px-3 py-1.5 gap-1.5">
    
        <span class="seedling-badge border border-[#FD6043] border-[1rem] w-5 h-5 rounded-full flex flex-col justify-center">
            <!-- Icon: Seedling (suggests origin/quality) -->
            <i class="fa-solid fa-mug-hot text-xl"></i>
        </span>
    
        <!-- MODIFICATION: Added limelight-regular class here to apply the font -->
        <div class="flex flex-col items-start limelight-regular z-50"> 
            <!-- Find My (Smaller Text - Minimalistic/Light) -->
            <div class="text-xs leading-none font-light">FIND MY</div> 
            <!-- Flavor (Larger Text - Minimalistic/Light) -->
            <div class="text-base leading-none font-light">FLAVOR</div> 
        </div>
        
        <!-- NEW TOOLTIP SPAN -->
        <!-- This is the tooltip. It's positioned relative to the parent 'a' tag. -->
        <!-- It's hidden by default (opacity-0) and becomes visible on 'group-hover' (opacity-100) -->
        <span class="tooltip absolute -left-4 top-1/2 -translate-y-1/2 -translate-x-full w-max 
                     bg-negroni-500 text-charcoal-500 text-xs font-semibold px-3 py-1.5 rounded-lg 
                     opacity-0 group-hover:opacity-100 transition-all duration-300 
                     pointer-events-none whitespace-nowrap kanit-medium normal-case tracking-normal">
            Need assistance? Chat with AI ->
        </span>
    </button>
</div>